# Lazy-load argo completion on first use
# This function will be replaced by the real argo after first invocation

# Load completion
eval "$(command argo completion zsh)"

# Remove this function so subsequent calls use real argo
unfunction argo

# Execute the real argo
command argo "$@"
