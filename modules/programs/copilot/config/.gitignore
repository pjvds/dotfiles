# Ignore all runtime state, logs, caches, and databases written to ~/.copilot
# which is symlinked directly to this directory by nix-darwin.
*

# Allowlist only the tracked configuration files
!.gitignore
!copilot-instructions.md
!mcp-config.json


