# enable uncommon matching
# this means if we search for `z code` we jump to `~/code` instead
# of the most popular subdirectory of `~/code` like `~/code/myproject`
export ZSHZ_UNCOMMON=1
zinit load agkozak/zsh-z
